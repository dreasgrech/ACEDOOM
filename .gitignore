# python
__pycache__/
*.pyc

# editor swap / temp files (install_app.py skips them too)
*.swp
*~

# build outputs (tools/build_wasm.py, pack_kspkg.py) and generated audio sources (tools/extract_sounds.py)
build/
dist/
audio/sfx/
audio/sfx44/
# and the prepared copies build_audio.py makes from them: same id Software lumps,
# normalised and padded. Regenerated from the player's own WAD, never shipped.
audio/sfx_padded/

# FMOD Studio project: Kunos's SDK modding template, copied in to rebuild the bank; not ours to
# redistribute. Only our own files under audio/ are committed (sounds.json, fmod/studio_template.js).
# To rebuild: copy the SDK template to audio/fmod/project/, run tools/build_audio.py, open in FMOD.
audio/fmod/project/
audio/acevo_content/
audio/sfx44/
